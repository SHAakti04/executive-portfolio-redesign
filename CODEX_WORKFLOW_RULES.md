# Codex Workflow Rules

- Do not directly edit source files.
- Always provide copy-paste code patches only.
- For every patch, clearly mention the file name.
- For every patch, show exactly which existing code should be replaced.
- For every patch, show exactly what replacement code should be pasted.
- Do not apply patches unless the user explicitly allows direct implementation again.
